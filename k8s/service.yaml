apiVersion: v1
kind: Service
metadata:
  name: device-booking-service
  namespace: default
spec:
  selector:
    app: device-booking-app
  ports:
    - port: 80
      targetPort: 8080
  type: NodePort